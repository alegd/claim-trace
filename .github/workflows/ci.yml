name: CI

on:
  push:
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm build
      - run: pnpm test

  review:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - name: Verify provider credentials
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
        run: |
          if [ -z "$CLAUDE_CODE_OAUTH_TOKEN" ]; then
            echo "::error::Missing repository secret CLAUDE_CODE_OAUTH_TOKEN. Generate it with 'claude setup-token' and add it in Settings > Secrets and variables > Actions."
            exit 1
          fi
      - run: npm install -g @anthropic-ai/claude-code
      - name: Install Gentleman Guardian Angel v2.10.1, pinned by commit
        env:
          GGA_COMMIT: 1124c3672f082c56b033c4e23a30e95d0e8cd593
        run: |
          git clone --no-checkout \
            https://github.com/Gentleman-Programming/gentleman-guardian-angel.git "$RUNNER_TEMP/gga"
          git -C "$RUNNER_TEMP/gga" checkout --detach "$GGA_COMMIT"
          echo "$RUNNER_TEMP/gga/bin" >> "$GITHUB_PATH"
      - run: gga run --ci
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
